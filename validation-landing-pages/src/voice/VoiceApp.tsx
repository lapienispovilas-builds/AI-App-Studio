import { useEffect, useRef, useState, type CSSProperties, type FormEvent } from 'react'
import { ArrowRight, ArrowDown, Check, FileText, Phone, MessageSquare, Menu, X, AudioLines } from 'lucide-react'
import { voiceBrand, voicePages, type VoicePageConfig } from './config'
import './voice.css'

function PilotLink({ children = 'Discuss a pilot' }: { children?: string }) {
  return <a className="v-button" href="#contact">{children}<ArrowRight size={17} aria-hidden="true" /></a>
}
function Header() {
  const [open, setOpen] = useState(false)
  const menu = useRef<HTMLButtonElement>(null)
  return <header className="v-header"><div className="v-container v-nav"><a className="v-brand" href="#top" aria-label={`${voiceBrand.name} back to top`}><AudioLines aria-hidden="true" />{voiceBrand.name}<span className="v-brand-dot">.</span></a><button ref={menu} className="v-menu" aria-label={open ? 'Close menu' : 'Open menu'} aria-expanded={open} aria-controls="voice-navigation" onClick={() => setOpen(!open)}>{open ? <X /> : <Menu />}</button><nav id="voice-navigation" className={open ? 'is-open' : ''} aria-label="Main navigation" onClick={() => setOpen(false)} onKeyDown={e => { if (e.key === 'Escape') { setOpen(false); menu.current?.focus() } }}><a href="#workflow">Workflow</a><a href="#pilot">Pilot</a><a href="#faq">FAQ</a><PilotLink /></nav></div></header>
}
function WorkflowIllustration({ config: c }: { config: VoicePageConfig }) {
  const conversationIndex = 1
  return <figure className="v-illustration"><figcaption><span className="v-dot" />Illustrative workflow<span>{c.inbound ? 'INBOUND OVERFLOW' : 'VOICE FOLLOW-UP'}</span></figcaption><div className="v-flow-stack">{c.illustrationLabels.map((label, i) => <div key={label}>{i > 0 && <div className="v-connector"><ArrowDown size={16} aria-hidden="true" /></div>}<div className={`v-flow-card ${i === conversationIndex ? 'v-call' : ''}`}><div className="v-call-heading"><span className="v-icon">{i === conversationIndex || (c.inbound && i === 0) ? <Phone size={21} aria-hidden="true" /> : i === 2 ? <MessageSquare size={21} aria-hidden="true" /> : <FileText size={21} aria-hidden="true" />}</span><div><small>0{i + 1} / {i === 0 ? 'THE START' : i === conversationIndex ? 'THE CONVERSATION' : c.channel.toUpperCase()}</small><strong>{label}</strong></div></div>{i === conversationIndex && <div className="v-wave" aria-hidden="true">{[8,14,20,12,26,18,22,10,24,16,20,10].map((height,j) => <i key={j} style={{height}} />)}</div>}</div></div>)}</div><div className="v-illustration-foot"><MessageSquare size={15} aria-hidden="true" />{c.outcomeDetail}</div></figure>
}
function PilotForm({ question, intro, heading }: { question: string; intro: string; heading: string }) {
  const [submitted, setSubmitted] = useState(false)
  const status = useRef<HTMLDivElement>(null)
  function submit(event: FormEvent<HTMLFormElement>) {
    event.preventDefault()
    setSubmitted(true)
  }
  useEffect(() => { if (submitted) status.current?.focus() }, [submitted])
  return <div className="v-form-card"><span className="v-label">LET’S EXPLORE THE FIT</span><h3>{heading}</h3><p>{intro}</p><form aria-describedby="preview-notice" onSubmit={submit} onChange={() => setSubmitted(false)}><div className="v-fields"><label htmlFor="pilot-name">Name<input id="pilot-name" name="name" autoComplete="name" required maxLength={120} pattern=".*\S.*" /></label><label htmlFor="pilot-company">Company<input id="pilot-company" name="company" autoComplete="organization" required maxLength={160} pattern=".*\S.*" /></label></div><label htmlFor="pilot-email">Work email<input id="pilot-email" type="email" name="email" autoComplete="email" required maxLength={254} /></label><label htmlFor="pilot-context">{question} <span>(optional)</span><textarea id="pilot-context" name="context" rows={3} maxLength={2000} /></label><button className="v-button" type="submit">Request a pilot conversation<ArrowRight size={17} aria-hidden="true" /></button><p className="v-preview-note" id="preview-notice">Preview only. This form does not send or save your information.</p>{submitted && <div ref={status} tabIndex={-1} className="v-form-status" role="status">This is a preview. Your fields passed validation, but no information was sent.</div>}</form></div>
}
function VoiceLandingPage({ config: c }: { config: VoicePageConfig }) {
  useEffect(() => { document.title = c.title; document.documentElement.lang = 'en'; document.querySelector('meta[name="description"]')?.setAttribute('content', c.metaDescription) }, [c])
  return <div className="voice-page" id="top" style={{ '--v-accent': c.accent, '--v-soft': c.soft } as CSSProperties}><a className="v-skip" href="#main">Skip to content</a><Header /><main id="main"><section className="v-container v-hero"><div className="v-hero-copy"><p className="v-eyebrow"><span className="v-dot" />{c.eyebrow}</p><h1>{c.headline}{' '}<span>{c.headlineAccent}</span></h1><p className="v-lead">{c.description}</p><div className="v-actions"><PilotLink /><a className="v-text-link" href="#workflow">See the workflow<ArrowDown size={16} aria-hidden="true" /></a></div><p className="v-hero-note">{c.invitation}</p></div><WorkflowIllustration config={c} /></section><section id="workflow" className="v-workflow-band"><div className="v-container v-section"><div className="v-section-heading"><div><p className="v-label">THE WORKFLOW</p><h2>{c.workflowTitle}</h2></div><p>{c.problem}</p></div><ol className="v-process">{c.workflow.map((step,i) => <li key={step.title}><div className="v-step-number">0{i+1}<ArrowRight size={19} aria-hidden="true" /></div><h3>{step.title}</h3>{step.body && <p>{step.body}</p>}</li>)}</ol></div></section><section className="v-example-band"><div className="v-container v-example v-section"><div><p className="v-label">A CONVERSATION WITH A PURPOSE</p><h2>{c.example.title}</h2><p className="v-lead-small">{c.example.note}</p></div><div className="v-transcript"><div className="v-transcript-head"><Phone size={17} /><strong>{c.workflowName}</strong><span>Illustrative conversation</span></div><p className="v-example-context">AI assistant {c.inbound ? 'answering for' : 'calling on behalf of'} [company]</p><div className="v-messages">{c.example.messages.map((m,i) => <div className={`v-message ${m.speaker !== 'AI assistant' ? 'v-customer' : ''}`} key={i}><small>{m.speaker}</small><p>{m.text}</p></div>)}</div><div className="v-message-confirm"><MessageSquare size={18} /><div><small>ILLUSTRATIVE {c.channel.toUpperCase()}{c.inbound ? ' ALERT' : ' FOLLOW-UP'}</small><p>{c.example.confirmation}</p></div></div></div></div></section><section className="v-container v-section v-value"><p className="v-label">WHAT WE’LL LEARN</p><h2>A practical pilot.<br />A clearer picture of the value.</h2><div className="v-value-list">{c.values.map((value,i) => <article key={value.title}><span>0{i+1}</span><h3>{value.title}</h3>{value.body && <p>{value.body}</p>}</article>)}</div></section><section id="pilot" className="v-container v-pilot-band"><div className="v-pilot"><div><p className="v-label">EARLY PILOT INVITATION</p><h2>{c.pilot.title}</h2><p className="v-lead-small">{c.pilot.description}</p></div><div><ul>{c.pilot.scope.map(s => <li key={s}><Check size={18} />{s}</li>)}</ul><p className="v-compatibility">We’ll review your software and messaging setup during the pilot discussion.</p><PilotLink /></div></div></section><section id="faq" className="v-container v-section v-faq"><div><p className="v-label">A FEW GOOD QUESTIONS</p><h2>Before we talk.</h2></div><div>{c.faqs.map(f => <details key={f.question}><summary>{f.question}<span aria-hidden="true">+</span></summary><p>{f.answer}</p></details>)}</div></section><section id="contact" className="v-closing-band"><div className="v-container v-closing v-section"><div><p className="v-label">LET’S EXPLORE A PILOT</p><h2>{c.finalTitle}</h2><p className="v-lead-small">One focused workflow. Calls, {c.channel === 'SMS' ? 'texts' : 'emails'} and a clear next step for your team.</p></div><PilotForm question={c.pilot.question} intro={c.formIntro} heading={c.formHeading} /></div></section></main><footer className="v-container v-footer"><a className="v-brand" href="#top"><AudioLines aria-hidden="true" />{voiceBrand.name}<span className="v-brand-dot">.</span></a><span>{c.inbound ? 'Voice-first intake. Human next steps.' : 'Voice-first follow-up. Human next steps.'}</span><span>Early pilot · {new Date().getFullYear()}</span></footer></div>
}
export function App() {
  const path = window.location.pathname.replace(/\/$/, '') as VoicePageConfig['path']
  const config = voicePages[path]
  useEffect(() => { if (!config) { document.title = `Page not available | ${voiceBrand.name}`; document.querySelector('meta[name="description"]')?.setAttribute('content', 'This voice AI industry page is not available yet.') } }, [config])
  return config ? <VoiceLandingPage config={config} /> : <main className="voice-page v-unavailable"><p className="v-label">NOT AVAILABLE YET</p><h1>This page is still to come.</h1><p>This workflow is not part of the current preview.</p><a href="/moving">View the moving pilot →</a></main>
}
